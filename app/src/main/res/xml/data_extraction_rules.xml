<?xml version="1.0" encoding="utf-8"?>
<!--
  Preserve user settings and phrase lists, but never cloud-back up or device-transfer the Room OTP
  history database. Declaring an include switches each transfer mode from Android's default of
  almost-all app data to this explicit allowlist.
-->
<data-extraction-rules>
  <cloud-backup disableIfNoEncryptionCapabilities="true">
    <include
        domain="file"
        path="datastore/." />
  </cloud-backup>
  <device-transfer>
    <include
        domain="file"
        path="datastore/." />
  </device-transfer>
</data-extraction-rules>
